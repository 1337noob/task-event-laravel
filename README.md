```shell
docker-compose up -d
```

```shell
cp .env.example .env
```

```shell
php artisan key:generate
```

```shell
php artisan migrate:fresh --seed
```

```shell
php artisan serve
```

Swagger:

http://localhost:8000/api/documentation
